import 'package:flutter/material.dart';
import 'package:trab_fs_2/screens/home_screen.dart';

void main() {
  runApp(MaterialApp(
      title: "App", debugShowCheckedModeBanner: false, home: HomeScreen()));
  // runApp(Game());
}
